import 'package:flutter/material.dart';

class MahasiswaRisikoScreen extends StatelessWidget {
  const MahasiswaRisikoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Mahasiswa Risiko',
        style: TextStyle(
          fontSize: 24,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}